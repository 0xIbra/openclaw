/**
 * Stub register.pairing — the full pairing module was removed per SPEC.md.
 */
import type { Command } from "commander";

export function registerNodesPairingCommands(_nodes: Command): void {
  // No-op: pairing commands removed.
}
